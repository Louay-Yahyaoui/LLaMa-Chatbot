# LLaMA Fine-Tuning

Two notebooks for LLaMA model fine-tuning.

- **`llama-finetuning.ipynb`** – fine-tunes a LLaMA model on custom educational data.  
- **`load_model.ipynb`** – loads a fine-tuned model for inference.

## Usage
 Run llama-finetuning.ipynb to train, then load_model.ipynb to test.
